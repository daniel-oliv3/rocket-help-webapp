import { HStack } from 'native-base';

export function Header() {
  return (
    <HStack
        w="full"
        justifyContent="space-between"
        alignItems="center"
        bg="gray.600"
        pb={6}
        pt={12}
    >

    </HStack>
  );
}